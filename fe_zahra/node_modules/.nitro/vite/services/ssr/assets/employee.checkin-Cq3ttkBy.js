import { t as Button } from "./button-CqVBJ60X.js";
import { i as getActiveEmployee, n as createAttendance } from "./db-_r_BwSlB.js";
import { i as useWorkMode, n as getVendor, r as haversine, t as OFFICE } from "./work-mode-BjZwHty8.js";
import { useEffect, useRef, useState } from "react";
import { useNavigate, useSearch } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
import { AlertTriangle, Camera, Check, Loader2, MapPin, RefreshCcw, X } from "lucide-react";
//#region src/routes/employee.checkin.tsx?tsr-split=component
function CheckinScreen() {
	const nav = useNavigate();
	const { type = "in" } = useSearch({ from: "/employee/checkin" });
	const attendanceType = type === "out" ? "Check-Out" : "Check-In";
	const profile = getActiveEmployee();
	const { mode } = useWorkMode();
	const assignedVendor = getVendor(profile?.vendor_id);
	const target = mode === "office" ? {
		lat: OFFICE.lat,
		lng: OFFICE.lng,
		radius: OFFICE.radius,
		name: OFFICE.name
	} : assignedVendor ? {
		lat: assignedVendor.latitude,
		lng: assignedVendor.longitude,
		radius: assignedVendor.radius_m,
		name: assignedVendor.name
	} : null;
	const videoRef = useRef(null);
	const canvasRef = useRef(null);
	const streamRef = useRef(null);
	const [cameraError, setCameraError] = useState(null);
	const [photo, setPhoto] = useState(null);
	const [coords, setCoords] = useState(null);
	const [gpsError, setGpsError] = useState(null);
	const [submitting, setSubmitting] = useState(false);
	useEffect(() => {
		let active = true;
		navigator.mediaDevices?.getUserMedia({
			video: { facingMode: "user" },
			audio: false
		}).then((stream) => {
			if (!active) {
				stream.getTracks().forEach((t) => t.stop());
				return;
			}
			streamRef.current = stream;
			if (videoRef.current) videoRef.current.srcObject = stream;
		}).catch(() => setCameraError("Tidak bisa mengakses kamera. Periksa izin kamera pada browser."));
		return () => {
			active = false;
			streamRef.current?.getTracks().forEach((t) => t.stop());
		};
	}, []);
	useEffect(() => {
		if (!navigator.geolocation) {
			setGpsError("Geolocation tidak didukung.");
			return;
		}
		navigator.geolocation.getCurrentPosition((pos) => setCoords({
			lat: pos.coords.latitude,
			lng: pos.coords.longitude
		}), () => setGpsError("Gagal membaca lokasi GPS."), { enableHighAccuracy: true });
	}, []);
	const distance = target && coords ? haversine(coords.lat, coords.lng, target.lat, target.lng) : null;
	const withinRadius = target && distance !== null ? distance <= target.radius : false;
	const capturePhoto = () => {
		const video = videoRef.current;
		const canvas = canvasRef.current;
		if (!video || !canvas) return;
		canvas.width = video.videoWidth || 480;
		canvas.height = video.videoHeight || 640;
		const ctx = canvas.getContext("2d");
		if (!ctx) return;
		ctx.translate(canvas.width, 0);
		ctx.scale(-1, 1);
		ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
		setPhoto(canvas.toDataURL("image/jpeg", .7));
	};
	const retake = () => setPhoto(null);
	const handleSubmit = () => {
		if (!profile) {
			toast.error("Profil karyawan tidak ditemukan.");
			return;
		}
		if (attendanceType === "Check-In") {
			if (!target) {
				toast.error("Anda belum terikat ke vendor manapun. Hubungi admin.");
				return;
			}
			if (!coords) {
				toast.error("Lokasi GPS belum terbaca, coba lagi.");
				return;
			}
			if (!withinRadius) {
				toast.error(`Di luar radius (${distance}m). Batas maksimal ${target.radius}m.`);
				return;
			}
			if (!photo) {
				toast.error("Ambil foto selfie terlebih dahulu sebagai bukti presensi.");
				return;
			}
		}
		setSubmitting(true);
		createAttendance({
			employee_id: profile.id,
			vendor_id: mode === "vendor" ? assignedVendor?.id ?? null : null,
			type: attendanceType,
			status: target && distance !== null ? distance <= target.radius ? "success" : "anomaly" : "success",
			distance_m: distance ?? 0,
			latitude: coords?.lat ?? 0,
			longitude: coords?.lng ?? 0,
			photo
		});
		toast.success(attendanceType === "Check-In" ? "Check-In Berhasil!" : "Check-Out Berhasil!");
		setSubmitting(false);
		nav({ to: "/employee" });
	};
	return /* @__PURE__ */ jsxs("div", {
		className: "min-h-screen bg-slate-950 text-white flex flex-col",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "p-4 flex items-center justify-between border-b border-slate-800",
			children: [
				/* @__PURE__ */ jsx("button", {
					onClick: () => nav({ to: "/employee" }),
					className: "p-2 -ml-2 rounded-full hover:bg-slate-800",
					children: /* @__PURE__ */ jsx(X, { className: "w-5 h-5" })
				}),
				/* @__PURE__ */ jsx("p", {
					className: "font-semibold text-sm",
					children: attendanceType === "Check-In" ? "Presensi Masuk" : "Presensi Keluar"
				}),
				/* @__PURE__ */ jsx("div", { className: "w-9" })
			]
		}), /* @__PURE__ */ jsxs("div", {
			className: "flex-1 flex flex-col items-center justify-center p-5 gap-4",
			children: [
				/* @__PURE__ */ jsx("div", {
					className: "relative w-full max-w-sm aspect-[3/4] rounded-3xl overflow-hidden bg-slate-900 border border-slate-800",
					children: photo ? /* @__PURE__ */ jsx("img", {
						src: photo,
						alt: "Foto presensi",
						className: "w-full h-full object-cover"
					}) : cameraError ? /* @__PURE__ */ jsxs("div", {
						className: "w-full h-full grid place-items-center p-6 text-center text-slate-400 text-sm gap-2",
						children: [/* @__PURE__ */ jsx(AlertTriangle, { className: "w-6 h-6 mx-auto text-amber-500 mb-2" }), cameraError]
					}) : /* @__PURE__ */ jsx("video", {
						ref: videoRef,
						autoPlay: true,
						playsInline: true,
						muted: true,
						className: "w-full h-full object-cover -scale-x-100"
					})
				}),
				/* @__PURE__ */ jsx("canvas", {
					ref: canvasRef,
					className: "hidden"
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "w-full max-w-sm rounded-2xl bg-slate-900 border border-slate-800 p-4 text-xs space-y-2",
					children: [
						/* @__PURE__ */ jsxs("div", {
							className: "flex items-center justify-between",
							children: [/* @__PURE__ */ jsxs("span", {
								className: "text-slate-400 flex items-center gap-1.5",
								children: [/* @__PURE__ */ jsx(MapPin, { className: "w-3.5 h-3.5" }), " Lokasi target"]
							}), /* @__PURE__ */ jsx("span", {
								className: "font-semibold",
								children: target?.name ?? "Belum ditentukan"
							})]
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "flex items-center justify-between",
							children: [/* @__PURE__ */ jsx("span", {
								className: "text-slate-400",
								children: "Jarak"
							}), /* @__PURE__ */ jsx("span", {
								className: `font-mono font-bold ${withinRadius ? "text-emerald-400" : "text-red-400"}`,
								children: gpsError ? "GPS error" : distance !== null ? `${distance} m` : "Membaca..."
							})]
						}),
						attendanceType === "Check-In" && target && distance !== null && !withinRadius && /* @__PURE__ */ jsxs("p", {
							className: "text-red-400",
							children: [
								"Di luar radius ",
								target.radius,
								"m — presensi akan ditolak."
							]
						})
					]
				}),
				photo ? /* @__PURE__ */ jsxs("div", {
					className: "w-full max-w-sm grid grid-cols-2 gap-3",
					children: [/* @__PURE__ */ jsxs(Button, {
						variant: "outline",
						className: "border-slate-700 text-white",
						onClick: retake,
						children: [/* @__PURE__ */ jsx(RefreshCcw, { className: "w-4 h-4 mr-1.5" }), " Ambil Ulang"]
					}), /* @__PURE__ */ jsxs(Button, {
						className: "bg-emerald-600 hover:bg-emerald-500",
						onClick: handleSubmit,
						disabled: submitting || attendanceType === "Check-In" && !withinRadius,
						children: [submitting ? /* @__PURE__ */ jsx(Loader2, { className: "w-4 h-4 mr-1.5 animate-spin" }) : /* @__PURE__ */ jsx(Check, { className: "w-4 h-4 mr-1.5" }), "Kirim"]
					})]
				}) : /* @__PURE__ */ jsxs(Button, {
					className: "w-full max-w-sm bg-indigo-600 hover:bg-indigo-500",
					onClick: capturePhoto,
					disabled: !!cameraError,
					children: [/* @__PURE__ */ jsx(Camera, { className: "w-4 h-4 mr-1.5" }), " Ambil Foto"]
				}),
				attendanceType === "Check-Out" && !photo && /* @__PURE__ */ jsx("button", {
					onClick: handleSubmit,
					className: "text-xs text-slate-400 underline underline-offset-2",
					children: "Lewati foto & langsung check-out"
				})
			]
		})]
	});
}
//#endregion
export { CheckinScreen as component };
